# Twitter Homepage Mockup

A react-based mock up of twitter's home page (unauthenticated).

## Installation

- Clone this repo
- `cd` into the repo: `$ cd twitter-homepage-mock`
- Install dependencies: `$ npm install`
- Start the app: `$ npm start`
